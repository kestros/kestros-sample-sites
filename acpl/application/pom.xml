<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.kestros.cms</groupId>
        <artifactId>kestros-cms-parent</artifactId>
        <version>0.1.2</version>
    </parent>

    <groupId>io.kestros.samples</groupId>
    <artifactId>acpl-application</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>bundle</packaging>

    <name>ACPL — Application</name>
    <description>ACPL UI framework, component views (layouts/variations), custom components, and dynamic-page filters.</description>

    <properties>
        <rootPackage>io.kestros.samples.acpl.application</rootPackage>
        <bundleCategory>kestros</bundleCategory>
    </properties>

    <dependencies>
        <!-- Test-scope only: slf4j has no binding in the inherited chain, so a test cannot see a
             log line without one. Nothing here ships in the bundle. -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>1.2.10</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.kestros.samples</groupId>
            <artifactId>acpl-api</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.kestros.cms</groupId>
            <artifactId>kestros-basic-components</artifactId>
            <version>0.3.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>io.kestros.cms</groupId>
            <artifactId>kestros-component-types-api</artifactId>
            <version>0.1.3-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>io.kestros.cms</groupId>
            <artifactId>kestros-site-building-api</artifactId>
            <version>0.1.4-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
        <!-- AbstractDynamicPageFilter — only present in site-building-core 0.1.6-SNAPSHOT -->
        <dependency>
            <groupId>io.kestros.cms</groupId>
            <artifactId>kestros-site-building-core</artifactId>
            <version>0.1.6-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <sourceDirectory>src/main/java</sourceDirectory>
        <testSourceDirectory>src/test/java</testSourceDirectory>
        <plugins>
            <plugin>
                <groupId>org.apache.jackrabbit</groupId>
                <artifactId>filevault-package-maven-plugin</artifactId>
                <version>1.3.6</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>generate-metadata</goal>
                            <goal>package</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <group>io.kestros.samples</group>
                    <packageType>mixed</packageType>
                    <accessControlHandling>merge</accessControlHandling>
                    <filterSource>${basedir}/src/content/META-INF/vault/filter.xml</filterSource>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <extensions>true</extensions>
                <configuration>
                    <instructions>
                        <Sling-Initial-Content>
                            /apps/kestros/commons/components/content/table/datasources;overwrite:=true;path:=/apps/kestros/commons/components/content/table/datasources,/apps/kestros/commons/components/lists/card-list/datasources;overwrite:=true;path:=/apps/kestros/commons/components/lists/card-list/datasources
                        </Sling-Initial-Content>
                        <Sling-Model-Packages>io.kestros.samples.acpl.application</Sling-Model-Packages>
                    </instructions>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>installBundle</id>
        </profile>
        <profile>
            <id>installPackage</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.wcm.maven.plugins</groupId>
                        <artifactId>wcmio-content-package-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>install</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <serviceURL>
                                ${sling.protocol}://${sling.host}:${sling.port}/bin/cpm/package.service.html
                            </serviceURL>
                            <userId>${sling.username}</userId>
                            <password>${sling.password}</password>
                            <packageFile>target/${project.artifactId}-${project.version}.zip
                            </packageFile>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
